1、锁池
假如thread-A获取到对象B的锁，那么当其他线程想获取对象B的锁时候，就进入到对象B的锁池。
2、等待池
假如thread-A调用wait方法（wait方法前已经获取对象B的锁），则会释放对象B的锁进入到对象B的等待池，其他线程调用同一个对象对象B的notify/notifyall，
则在等待池的线程就会进入到锁池争抢锁资源。特此注明调用notify/notifyall 不会立即释放锁，需要等同步块结束。
